---
name: design-to-issues
description: Create GitHub implementation issues (Epic + REQ sub-issues) from an approved design.
---

Use the `design-to-issues` skill after the design PR is merged or explicitly approved.

Example: `user-auth` の実装Issueを作って
